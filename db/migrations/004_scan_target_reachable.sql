-- 포트 응답 여부. 범위 스캔은 대부분이 닫힌 포트라 실제 스캔 대상과 요청 대상이 다르다.
-- NULL = 확인 안 함, 1 = 응답, 0 = 무응답
--
-- 이 값이 없으면 '401개를 요청했는데 3개만 실제로 봤다' 를 보고서가 밝힐 수 없고,
-- 요청 수만 적으면 점검 범위가 과장된다 (절대규칙 10)
ALTER TABLE scan_targets ADD COLUMN reachable INTEGER;
